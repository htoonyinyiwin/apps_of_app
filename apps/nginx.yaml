# Phase 2: Replaced by ApplicationSet (nginx-appset.yaml)
# ApplicationSet generates one Application per environment (dev/staging/prod) automatically.
# This single Application deployed nginx to one namespace — no multi-env support.
#
# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: nginx
#   namespace: argocd
# spec:
#   project: default
#   source:
#     repoURL: https://github.com/htoonyinyiwin/apps_of_app.git
#     targetRevision: main
#     path: manifests/nginx
#   destination:
#     server: https://kubernetes.default.svc
#     namespace: nginx
#   syncPolicy:
#     automated:
#       prune: true
#       selfHeal: true
#     syncOptions:
#       - CreateNamespace=true
